services:
  db:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_DB: trafficscore
      POSTGRES_USER: trafficscore
      POSTGRES_PASSWORD: trafficscore
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/schema.sql:/docker-entrypoint-initdb.d/schema.sql

volumes:
  pgdata:
